<!DOCTYPE html>
<html>
<head>
    <title>Pokémon Cards</title>
    <style>
        *{
            font-family: 'Pokemon Solid', sans-serif;
        }
        .pokemon-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .pokemon-card {
            width: calc(25% - 20px);
            min-width: 200px;
            padding: 8px;
            border-radius: 12px;
            box-shadow: 2px 3px 10px rgba(0,0,0,0.3);
            text-align: center;
            color: black;
            position: relative;
            border: 8px solid #DAA520;
            background-color: #f8f8f8;
            padding-top: 35px;
        }

        .pokemon-card::before {
            content: '';
            position: absolute;
            top: 8px;
            left: 8px;
            right: 8px;
            bottom: 8px;
            border-radius: 6px;
            border: 2px solid rgba(0,0,0,0.1);
            pointer-events: none;
        }

        .pokemon-card img {
            width: 150px;
            height: 150px;
            object-fit: contain;
            margin: 5px auto;
            border: 3px solid #e0e0e0;
            border-radius: 4px;
            background: #ffffff;
            display: block;
        }

        .pokemon-card h3 {
            margin: 5px 0;
            font-size: 1.2em;
            color: #333;
        }

        .pokemon-info {
            text-align: left;
            padding: 5px;
            margin: 5px;
            background-color: rgba(255, 255, 255, 0.3);
            border-radius: 4px;
        }

        .attack-info {
            margin: 5px 0;
            padding: 8px;
            background-color: rgba(255, 255, 255, 0.3);
            border-radius: 4px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .attack-name-desc {
            flex-grow: 1;
        }

        .attack-name-desc strong {
            font-size: 1em;
        }

        .attack-name-desc .description {
            font-size: 0.9em;
            color: #444;
            margin-left: 4px;
        }

        .attack-power {
            margin-left: 10px;
            white-space: nowrap;
        }

        .rarity-info {
            margin: 5px 0;
            padding: 5px;
            font-size: 0.9em;
            color: #666;
            border-top: 1px solid rgba(0, 0, 0, 0.1);
        }

        .pokemon-info p {
            margin: 3px 0;
            font-size: 0.9em;
        }

        .pokemon-info p:nth-child(2) {
            font-weight: bold;
            color: #444;
        }

        .pokemon-info p:first-child {
            color: black;
            font-weight: bold;
        }

        .Normal{
    background: #A9A979;
  }
  .Feu{
    background: #F08030;
  }
  .Eau{
    background: #6890F0;
  }
  .Plante{
    background: #78C850;
  }
  .Électrik{
    background: #F8D030;
  }
  .Glace{
    background: #98D8D8;
  }
  .Combat{
    background: #C03028;
  }
  .Poison{
    background: #A040A0;
  }
  .Sol{
    background: #E0C068;
  }
  .Fée{
    background: #EFA1AF;
  }
  .Psy{
    background: #F85888;
  }
  .Insecte{
    background: #A8B820;
  }
  .Roche{
    background: #B8A038;
  }
  .Spectre{
    background: #705898;
  }
  .Dragon{
    background: #7038F8;
  }

  .hp-display {
    position: absolute;
    top: 10px;
    right: 15px;
    color: #790000;
    font-weight: bold;
    font-size: 1.1em;
  }

  .pokemon-name {
    position: absolute;
    top: 10px;
    left: 15px;
    font-weight: bold;
    font-size: 1.1em;
    color: #333;
  }
    </style>
    <!-- Vos styles de types existants -->
</head>
<body>
    <div class="pokemon-container">
        <% pokemons.forEach(pokemon => { %>
            <div class="pokemon-card <%= pokemon.type %>">
                <div class="pokemon-name"><%= pokemon.name %></div>
                <div class="hp-display"><%= pokemon.hp %> HP</div>
                <img src="<%= pokemon.image %>" alt="<%= pokemon.name %>">
                <div class="pokemon-info">
                    <div class="attack-info">
                        <div class="attack-name-desc">
                            <strong><%= pokemon.attack.name %></strong>
                            <span class="description"><%= pokemon.attack.description %></span>
                        </div>
                        <div class="attack-power">
                            <%= pokemon.attack.power %>
                        </div>
                    </div>
                    
                    <div class="rarity-info">
                        <p><%= pokemon.rarity.label.charAt(0).toUpperCase() + pokemon.rarity.label.slice(1) %></p>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>
